import 'reflect-metadata';
import { expect } from 'chai';
import 'mocha';

describe('IMUCalibrationService', () => {
    describe('calibrate()', () => {
        
        it('should nullify readings', (done) => {

        });

    });
});
